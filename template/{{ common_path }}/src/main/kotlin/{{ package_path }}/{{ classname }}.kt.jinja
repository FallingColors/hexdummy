package {{ kotlin_package }}

import net.minecraft.resources.ResourceLocation
import org.apache.logging.log4j.LogManager
import org.apache.logging.log4j.Logger
import {{ kotlin_package }}.config.{{ classname }}ServerConfig
import {{ kotlin_package }}.networking.{{ classname }}Networking
import {{ kotlin_package }}.registry.{{ classname }}Actions

object {{ classname }} {
    const val MODID = "{{ modid }}"

    @JvmField
    val LOGGER: Logger = LogManager.getLogger(MODID)

    @JvmStatic
    fun id(path: String) = ResourceLocation(MODID, path)

    fun init() {
        {{ classname }}ServerConfig.init()
        initRegistries(
            {{ classname }}Actions,
        )
        {{ classname }}Networking.init()
    }

    fun initServer() {
        {{ classname }}ServerConfig.initServer()
    }
}
