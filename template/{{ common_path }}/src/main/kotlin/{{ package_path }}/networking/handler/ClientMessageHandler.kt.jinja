package {{ package }}.networking.handler

import dev.architectury.networking.NetworkManager.PacketContext
import {{ package }}.config.{{ classname }}Config
import {{ package }}.networking.msg.*

fun {{ classname }}MessageS2C.applyOnClient(ctx: PacketContext) = ctx.queue {
    when (this) {
        is MsgSyncConfigS2C -> {
            {{ classname }}Config.onSyncConfig(serverConfig)
        }

        // add more client-side message handlers here
    }
}
