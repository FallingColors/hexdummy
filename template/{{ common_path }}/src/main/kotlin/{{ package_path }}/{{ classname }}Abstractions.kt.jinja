@file:JvmName("{{ classname }}Abstractions")

package {{ kotlin_package }}

import dev.architectury.injectables.annotations.ExpectPlatform
import {{ kotlin_package }}.registry.{{ classname }}Registrar

fun initRegistries(vararg registries: {{ classname }}Registrar<*>) {
    for (registry in registries) {
        initRegistry(registry)
    }
}

@ExpectPlatform
fun <T : Any> initRegistry(registrar: {{ classname }}Registrar<T>) {
    throw AssertionError()
}
